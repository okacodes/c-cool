#include <stdio.h>

// the quintessential Hello World program
int main(void) 
{
    printf("Hello world!\n");

    // VARIABLES
    /* both of the examples below are considered
    valid examples of variables how you can define
    a variable. 

    int j is initialized, then assigned a variable. 
    int i is assigned immediately upon initialization */   

    // int j;
    // j = 3;
    // int i = 2; 

    
    // BOOLEANS
    int x = 1; // non zero-ish values(usually) are considered true, while zero-ish values are considered false

    if (x) {
        printf("x is true!\n");
    }

    // PRE-AND-POST INCREMENT-AND-DECREMENT
    int i = 0
}